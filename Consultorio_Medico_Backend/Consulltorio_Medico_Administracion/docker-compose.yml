version: '3.8'
## Docker para subir un servidor de base de datos mariadb de prueba para desarrollar el microservicio
services:
  mariadb:
    image: mariadb:11
    container_name: mariadb-dev
    environment:
      MARIADB_ROOT_PASSWORD: 200232
      MARIADB_DATABASE: hospital
    ports:
      - "3307:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

  mariadb-master2:
    image: mariadb:11
    container_name: mariadb-dev-master2
    environment:
      MARIADB_ROOT_PASSWORD: 200232
      MARIADB_DATABASE: hospital
    ports:
      - "3317:3306"
    volumes:
      - mariadb_master2_data:/var/lib/mysql

volumes:
  mariadb_data:
  mariadb_master2_data:
